{
  "name": "bitcoin-prediction-platform",
  "version": "1.0.0",
  "description": "比特币价格预测平台",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-frontend": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install",
    "install-python-deps": "pip install pandas numpy joblib scikit-learn requests python-dotenv"
  },
  "dependencies": {
    "axios": "^0.21.4",
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.21.2",
    "mysql2": "^2.3.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
